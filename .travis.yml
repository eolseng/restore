dist: trusty

matrix:
  include:
    - language: node.js
      node_js: 12
      before_script:
        - cd frontend
        - yarn install
      script:
        - yarn build
#    - language: java
#      jdk: openjdk11
#      before_script: cd backend
#      script: mvn clean verify --fae
