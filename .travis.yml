dist: trusty

matrix:
  include:
    - language: node.js
      node_js: 12
      before_script:
        - cd frontend
      script:
        - yarn install
#    - language: java
#      jdk: openjdk11
#      before_script: cd backend
#      script: mvn clean verify --fae
